- name: Demo on when condition
  hosts: localhost
  vars:
    URL: google.com
  tasks:
    - name: Condition on strings
      when: URL == "google.com"
      debug:
        msg: GOOOOOGLE

    - name: Condition on strings
      when: URL != "yahoo.com"
      debug:
        msg: YAHOOOOOO